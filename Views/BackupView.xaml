<UserControl x:Class="StokTakip.Views.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Border Background="White" CornerRadius="15" Padding="30" Margin="20" Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Yedekleme ve Geri Yükleme" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,10"/>
            <TextBlock Grid.Row="1" Text="Verilerinizi güvende tutmak için düzenli olarak yedek alın." FontSize="16" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,30"/>

            <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Left">
                
                <!-- Export Section -->
                <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="20" Width="600" HorizontalAlignment="Left" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="📤" FontSize="32" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Verileri Dışarı Aktar (Yedekle)" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryTextBrush}"/>
                            <TextBlock Text="Mevcut veritabanını bilgisayarınıza bir dosya (.db) olarak kaydeder." FontSize="14" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Grid.Column="2" Content="Yedekle" Click="BtnExport_Click" Width="120" Height="40" Style="{StaticResource PrimaryButtonStyle}" Margin="20,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Import Section -->
                <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="20" Width="600" HorizontalAlignment="Left" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="📥" FontSize="32" VerticalAlignment="Center" Margin="0,0,20,0"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Verileri İçeri Aktar (Geri Yükle)" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryTextBrush}"/>
                            <TextBlock Text="Daha önce aldığınız bir yedeği yükler. Mevcut veriler silinir!" FontSize="14" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Grid.Column="2" Content="Geri Yükle" Click="BtnImport_Click" Width="120" Height="40" Background="#F59E0B" Foreground="White" BorderThickness="0" Margin="20,0,0,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- Info Note -->
                 <Border Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="8" Padding="15" Width="600" HorizontalAlignment="Left" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ℹ️" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="İpucu: 'Verileri Sıfırla' işlemi yapmadan önce mutlaka yedek almanız önerilir." VerticalAlignment="Center" Foreground="#1E40AF" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>
