<UserControl x:Class="StokTakip.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <!-- Card Container -->
        <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#E2E8F0" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Ürünler" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource TextPrimary}"/>

                <DataGrid x:Name="GridProducts" Grid.Row="1" ColumnWidth="*" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Name}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Mevcut Stok" Binding="{Binding CurrentStock}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Birim" Binding="{Binding Unit}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Kritik Stok" Binding="{Binding CriticalStockLevel}" Width="100" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="İşlemler" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button x:Name="BtnEdit" Content="Düzenle" Click="BtnEdit_Click" 
                                            Style="{StaticResource GhostBtnStyle}" FontSize="12" Padding="8,4" Margin="0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination Controls -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <TextBlock Text="Sayfa Başına:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox x:Name="CmbPageSize" Width="70" Margin="0,0,20,0" SelectionChanged="CmbPageSize_SelectionChanged" SelectedIndex="0" VerticalAlignment="Center" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="10"/>
                        <ComboBoxItem Content="20"/>
                        <ComboBoxItem Content="50"/>
                        <ComboBoxItem Content="100"/>
                        <ComboBoxItem Content="Tümü"/>
                    </ComboBox>

                    <Button x:Name="BtnPrevPage" Content="&lt;" Style="{StaticResource GhostBtnStyle}" Width="35" Height="30" Margin="0,0,5,0" Click="BtnPrevPage_Click" IsEnabled="False"/>
                    <TextBlock x:Name="TxtPageInfo" Text="1 / 1" VerticalAlignment="Center" FontSize="14" Margin="10,0" Foreground="{StaticResource TextPrimary}"/>
                    <Button x:Name="BtnNextPage" Content="&gt;" Style="{StaticResource GhostBtnStyle}" Width="35" Height="30" Click="BtnNextPage_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Edit Overlay -->
        <Grid x:Name="PnlEditOverlay" Background="#90000000" Visibility="Collapsed" Grid.RowSpan="3">
            <Border Background="White" HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="12" Padding="30" Width="400">
                <StackPanel>
                    <TextBlock Text="Ürün Düzenle" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource TextPrimary}"/>
                    
                    <TextBlock Text="Ürün Adı" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtEditName" Margin="0,0,0,15"/>

                    <TextBlock Text="Birim (KG, Adet, Lt...)" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbEditUnit" Margin="0,0,0,15" Padding="10">
                        <ComboBoxItem Content="KG"/>
                        <ComboBoxItem Content="Adet"/>
                        <ComboBoxItem Content="Litre"/>
                    </ComboBox>

                    <TextBlock Text="Kritik Stok Seviyesi" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtEditCriticalStock" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="İptal" Click="BtnCancelEdit_Click" Style="{StaticResource GhostBtnStyle}" Margin="0,0,10,0"/>
                        <Button Content="Kaydet" Click="BtnSaveEdit_Click" Style="{StaticResource GreenBtnStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
