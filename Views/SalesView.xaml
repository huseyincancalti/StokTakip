<UserControl x:Class="StokTakip.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="30" Margin="0,0,0,20" VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect Color="#E2E8F0" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
            <StackPanel>
                <TextBlock Text="Stok Çıkış" FontSize="20" FontWeight="Bold" Margin="0,0,0,25" Foreground="{StaticResource TextPrimary}"/>

                <!-- Product Selection Row -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="Ürün Seç" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                            <ComboBox x:Name="CmbProducts" DisplayMemberPath="Name" SelectedValuePath="Id" Height="45" Padding="10" SelectionChanged="CmbProducts_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Mevcut Stok" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                            <Border Background="#F1F5F9" CornerRadius="8" Height="45" Padding="10">
                                <TextBlock x:Name="TxtCurrentStock" Text="-" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Quantity Input -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Çıkış Miktarı" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                        <TextBox x:Name="TxtQuantity" Height="45" FontSize="16" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    

                <!-- Action Button -->
                <Button Content="➖ Stok Çıkış Yap" Click="BtnSale_Click" Style="{StaticResource GreenBtnStyle}" 
                        HorizontalAlignment="Right" Width="220" Height="45" FontSize="16"/>
            </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
